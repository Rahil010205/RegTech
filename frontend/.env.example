NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Required to deliver OTPs to a real inbox (Gmail App Password, not your Google login password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
